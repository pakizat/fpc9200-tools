{
  "name": "fpc9200-dataset",
  "version": "1.0.0",
  "description": "FPC 9200 fingerprint dataset collection and matching analysis tool",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json",
    "start": "npm run build && electron .",
    "cli": "python3 fpc9200-dataset.py",
    "dev": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json && electron ."
  },
  "dependencies": {
    "electron-store": "^8.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "electron": "^28.2.0",
    "typescript": "^5.3.3"
  }
}
